import SwiftUI

/// Vue de détail d'un trajet — stub en attente d'implémentation.
struct JourneyDetailView: View {
    let journey: Journey

    var body: some View {
        Text(journey.title)
            .navigationTitle("Trajet")
    }
}
